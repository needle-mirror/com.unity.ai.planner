{
    "name": "{{ namespace }}",
    "references": [
        "Unity.AI.Planner",
        "Unity.Entities",
        "Unity.Jobs",
        "Unity.Burst",
        "Unity.Collections",
        "Unity.FullDotNet",
        "{{ domains_namespace }}"
        {{- for p in additional_references }}
        ,"{{ p }}"
        {{- end }}
    ],
    "defineConstraints": [
        "PLANNER_DOMAINS_GENERATED"
    ],
    "versionDefines": [
        {
            "name": "com.unity.{{ domains_namespace | string.downcase }}.generated",
            "expression": "0.0",
            "define": "PLANNER_DOMAINS_GENERATED"
        }
    ]
}
